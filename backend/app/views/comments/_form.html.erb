<%= form_with(model: [post, post.comments.build]) do |f| %>
    <div class="form-control">
        <%= f.rich_text_area :body %>
        <%= f.submit "Add Comment", class:"btn btn-primary my-3" %>
    </div>
<% end %>